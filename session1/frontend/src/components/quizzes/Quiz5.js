import React from "react";

import Typography from "@mui/material/Typography";
import { Box } from "@mui/material";

const Quiz5 = () => {
  return (
    <Box>
      <Typography variant="body1">Make a PR.</Typography>
    </Box>
  );
};

export default Quiz5;
